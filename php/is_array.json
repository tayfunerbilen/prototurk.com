{
  "author": "serkanosna <serkan.osna@gmail.com>",
  "title": "PHP'de is_array() Fonksiyonu",
  "name": "is_array",
  "syntax": "is_array ( mixed $değişken ) : bool",
  "description": "Belirtilen değişkenin dizi olup olmadığını kontrol eder.",
  "compatibilities": [
    "php 4",
    "php 5",
    "php 7"
  ],
  "params": [
    {
      "name": "$degisken",
      "type": "array",
      "description": "Kontrol edilecek değişken",
      "default": "$degisken",
      "required": true
    }
  ],
  "examples": [
    {
      "code": "$arr= array('bu', 'bir', 'dizidir');\necho is_array($arr) ? 'Dizi' : 'Dizi Değil';\n// Çıktı : Dizi\n\n$arr_2= 'bu bir dizgedir';\necho is_array($arr_2) ? 'Dizi' : 'Dizi Değil';\n// Çıktı : Dizi Değil",
      "description": "Değişken içeriğinin bir dizi olup olmadığının sınanması"
    }
  ],
  "return": {
    "type": "boolean",
    "text": "Belirtilen değişken dizi içeriyorsa <code>true</code> içermiyorsa <code>false</code> değeri döner."
  },
  "deprecated": false
}
